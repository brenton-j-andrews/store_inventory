extends layout 
 
block content 

    div.return-link-wrapper
        a(href="/" class="return-link") Return Home
        a(href=product_info.category.url class="return-link") Return to #{product_info.category.name} Page.

    div(class="categories-nav")
        h1=title

        div(class="category-crud-btns")
           
            a(href=`/product/${product_info._id}/update` class="btn-wrapper")
                img(src="/images/update.png"  alt="update" class="img-btn")
                span(class="tooltiptext") Edit this product.

            a(href=`/product/${product_info._id}/delete` class="btn-wrapper")
                img(src="/images/delete.png"  alt="delete" class="img-btn")
                span(class="tooltiptext") Delete this product.

    div(class="product-details")
        p Description: #{product_info.description}
        p Currently in stock: #{product_info.inventory}
        p Current price: $#{product_info.price}
        p Category: #{product_info.category.name}